# 能耗智能管理优化平台 — 部署指南

> 版本：v1.0 | 适用于 Windows / Linux / macOS

## 目录

1. [开发环境部署](#1-开发环境部署)
2. [生产环境部署](#2-生产环境部署)
3. [Docker 部署](#3-docker-部署)
4. [环境变量配置](#4-环境变量配置)
5. [常见问题](#5-常见问题)

---

## 1. 开发环境部署

### Windows

#### 后端

```powershell
cd backend

# 创建虚拟环境
python -m venv .venv
.venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 初始化数据库
python scripts/init_db.py

# 生成模拟数据（可选）
python scripts/init_mock_data.py

# 启动服务（热重载模式）
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

后端运行后访问：
- API 文档：http://localhost:8000/docs
- ReDoc 文档：http://localhost:8000/redoc

#### 前端

```powershell
cd frontend
npm install
npm run dev
```

浏览器访问：http://localhost:5173

### Linux / macOS

```bash
cd backend

python3 -m venv .venv
source .venv/bin/activate

pip install -r requirements.txt
python scripts/init_db.py
python scripts/init_mock_data.py

uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

```bash
cd frontend
npm install
npm run dev
```

---

## 2. 生产环境部署

### 2.1 后端部署（Nginx + Uvicorn）

```bash
# 安装生产依赖
cd backend
pip install -r requirements.prod.txt

# 多 worker 启动
uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
```

### 2.2 前端构建

```bash
cd frontend
npm install
npm run build
# 产物在 dist/ 目录
```

### 2.3 Nginx 反向代理

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    root /path/to/frontend/dist;
    index index.html;

    # SPA 路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 360s;      # Coze 工作流耗时较长
        proxy_send_timeout 360s;
    }
}
```

### 2.4 systemd 服务配置

创建 `/etc/systemd/system/energy-optimizer.service`：

```ini
[Unit]
Description=Energy Optimizer Backend
After=network.target

[Service]
Type=simple
User=appuser
Group=appgroup
WorkingDirectory=/opt/energy-optimizer/backend
EnvironmentFile=/opt/energy-optimizer/backend/.env
ExecStart=/opt/energy-optimizer/backend/.venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable energy-optimizer
sudo systemctl start energy-optimizer
sudo systemctl status energy-optimizer
```

### 2.5 日志管理

Uvicorn 日志默认输出到 stdout，可通过 journald 查看：

```bash
journalctl -u energy-optimizer -f
```

---

## 3. Docker 部署

### 3.1 后端 Dockerfile

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY backend/requirements.prod.txt .
RUN pip install --no-cache-dir -r requirements.prod.txt

COPY backend/ .

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "4"]
```

### 3.2 前端 Dockerfile

```dockerfile
FROM node:18-alpine AS build
WORKDIR /app
COPY frontend/package*.json ./
RUN npm ci
COPY frontend/ .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### 3.3 docker-compose.yml

```yaml
version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/energy_optimizer.db
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
```

启动：

```bash
docker-compose up -d
```

---

## 4. 环境变量配置

在后端目录创建 `.env` 文件：

```env
# 数据库
DATABASE_URL=sqlite:///./energy_optimizer.db

# 调试模式
DEBUG=false

# Coze 智能体平台配置
COZE_API_KEY=pat-your-api-key
COZE_API_BASE=https://api.coze.cn
COZE_ANALYZE_WORKFLOW_ID=wf-your-analyze-id
COZE_OPTIMIZE_WORKFLOW_ID=wf-your-optimize-id
COZE_CHAT_BOT_ID=bot-your-bot-id
COZE_AGENT_TIMEOUT=120
COZE_CHAT_TIMEOUT=30
```

| 变量名 | 说明 | 默认值 | 必填 |
|------|------|--------|:--:|
| `DATABASE_URL` | 数据库连接 URL | `sqlite:///./energy_optimizer.db` | 否 |
| `DEBUG` | 调试模式开关 | `false` | 否 |
| `COZE_API_KEY` | Coze API 访问令牌 | - | 否* |
| `COZE_API_BASE` | Coze API 端点 | `https://api.coze.cn` | 否 |
| `COZE_ANALYZE_WORKFLOW_ID` | 能耗分析工作流 ID | - | 否* |
| `COZE_OPTIMIZE_WORKFLOW_ID` | 调度优化工作流 ID | - | 否* |
| `COZE_CHAT_BOT_ID` | 对话智能体 Bot ID | - | 否* |
| `COZE_AGENT_TIMEOUT` | 工作流调用超时（秒） | `120` | 否 |
| `COZE_CHAT_TIMEOUT` | 对话智能体超时（秒） | `30` | 否 |

> *不填则自动降级为本地规则引擎模式，系统仍可正常使用。

---

## 5. 常见问题

### Q: 启动后端时提示端口被占用？

```bash
# Windows — 查找占用 8000 端口的进程
netstat -ano | findstr :8000
taskkill /PID <PID> /F

# Linux — 查找占用 8000 端口的进程
lsof -i :8000
kill -9 <PID>
```

### Q: 前端无法访问后端 API？

检查 `frontend/vite.config.js` 中的代理配置：

```js
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8000',
      changeOrigin: true,
    },
  },
}
```

确保后端已在 8000 端口启动。

### Q: Coze 工作流超时？

- 增加 `COZE_AGENT_TIMEOUT` 值（默认 120 秒）
- 检查 Coze 平台工作流状态
- 系统会自动降级到本地模式，不影响使用

### Q: 数据库文件在哪里？

默认在 `backend/energy_optimizer.db`。可通过 `DATABASE_URL` 环境变量修改路径。

### Q: 如何重置数据库？

```bash
cd backend
del energy_optimizer.db      # Windows
rm energy_optimizer.db       # Linux/macOS
python scripts/init_db.py
python scripts/init_mock_data.py
```
