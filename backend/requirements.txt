# 能耗智能管理优化平台 — 开发环境依赖
# =============================================

# Web 框架 — 高性能异步 REST API
fastapi==0.104.1

# ASGI 服务器 — 支持热重载
uvicorn[standard]==0.24.0

# ORM — 跨数据库兼容
SQLAlchemy==2.0.23

# 数据验证 — 自动生成 OpenAPI Schema
pydantic==2.13.0

# 环境变量管理
python-dotenv==1.0.0

# HTTP 客户端 — 异步调用智能体 API
httpx==0.28.1

# Coze SDK — 扣子智能体平台官方 SDK
cozepy==0.20.0

# Excel 导出 — 报表生成
openpyxl==3.1.2

# 密码安全 — bcrypt 哈希（bcrypt<5.0 兼容 passlib 1.7.4）
passlib[bcrypt]==1.7.4
bcrypt<5.0

# ========== 开发工具 ==========

# 测试框架
pytest==7.4.3
pytest-cov==4.1.0

# 代码质量
flake8==6.1.0
black==23.12.0
isort==5.13.2
mypy==1.7.1

# 表单数据解析 — FastAPI Form 参数必需
python-multipart>=0.0.12

# ========== 新增 RAG 知识库依赖 ==========

# LangChain 核心框架 — RAG 组件
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# 向量存储 — ChromaDB（文件级持久化）
langchain-chroma>=0.2.0
chromadb>=0.6.0

# LLM 接入 — OpenAI 兼容接口 + Ollama 本地模型
langchain-openai>=0.3.0
langchain-ollama>=0.3.0

# 文档解析 — PDF 加载器
pypdf>=5.0.0
